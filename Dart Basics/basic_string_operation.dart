void main() {
  String title = 'Dart Course';
  print(title);

  /*
   if I want to print what I have filled inside of the variable 'title' in a uppercase way,
   I need to sign an method inside an object. For example. object.method or 

    print is a function.
    
    print(title.toUppercase); // Object(title) Method(toUppercase)
    Lower case is the same thing. 
  */
  print(title.toUpperCase());
  print(title.toLowerCase());
}
